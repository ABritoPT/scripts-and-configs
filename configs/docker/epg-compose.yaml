services:
  epg:
    container_name: epg
    image: ghcr.io/iptv-org/epg:master
    restart: unless-stopped
    ports:
      - 8410:3000
    environment:
      - TZ=Europe/Berlin
      - DAYS=2
    volumes:
      - /mnt/Storage_2TB/main/IPTV/epg-channels.xml:/epg/channels.xml
